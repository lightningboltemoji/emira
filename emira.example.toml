# Every setting emira has, at its default — so a file that says none of this says exactly this.
# Write down only what you disagree with: an absent key keeps its default, and a key emira does not
# know is an error naming its line rather than a setting that quietly does nothing.
#
# Generated from the schema (`ConfigSchema.document`), and pinned by a test. Don't edit it by hand.

[layout]

# Points between adjacent columns on the strip.
# In points, must be at least 0.
column-gap = 0

# Points between windows stacked in one column.
# In points, must be at least 0.
window-gap = 0

# Centre a focused column rather than scrolling the least that reveals it.
center-focused-column = false

# Stop a grow or shrink where the columns on screen sit flush; press again to push past.
resize-detent = false

# The widths cycle-width steps through.
# A list of sizes — 1 or less is a fraction of the extent it is resolved against, anything
# larger is a point count.
width-presets = [0.3333333333333333, 0.5, 0.6666666666666666]

# The heights cycle-height steps through, inside the column.
# A list of sizes — 1 or less is a fraction of the extent it is resolved against, anything
# larger is a point count.
height-presets = [0.3333333333333333, 0.5, 0.6666666666666666]

# Points of margin held clear at the edges of the working area. Not a strut: a strut is forbidden
# ground, an outer gap is empty at rest and crossed in motion.
# In points, must be at least 0.
outer-gap = 0

# …and outer-gap-left, outer-gap-bottom, outer-gap-right — each replacing one side of it. A side on
# its own means that side, so outer-gap-left alone leaves the other three where they were.
# In points, must be at least 0.
outer-gap-top = 0

[focus]

# Which focus changes emira did not cause it honours.
# One of "respect", "on-screen", "ignore".
system-events = "respect"

# Focus a window when the pointer crosses into it. Here focus scrolls, so this moves the strip.
follows-mouse = false

[mouse]

# Hide the pointer while you work from the keyboard, until the mouse moves. The Dock unhides it.
hide = false

# Send the pointer after focus; except-hover skips a hover, lazy one already inside.
# One of "off", "lazy", "except-hover", "force".
follows-focus = "off"

[animation]

# Whether a cover is used during window transitions, and if so, what style.
# One of "off", "snap", "smooth".
transition = "smooth"

# Seconds a cover may stay up waiting for a window to land.
# In seconds, must be greater than 0.
hold-timeout = 1

# How a window's still is painted into a rect it no longer fits.
# One of "stretch", "crop".
window = "stretch"

# Whether a cover waits for every window's still, or goes up on the desktop's.
# One of "exact", "immediate".
cover = "exact"

[animation.scroll]

# Spring constant k for the viewport scroll — larger is stiffer, and faster.
# Must be greater than 0.
stiffness = 800

# ζ for the viewport scroll — 1 settles without overshoot, less overshoots.
# Must be at least 0.
damping-ratio = 1

[animation.resize]

# Spring constant k for a column's width — larger is stiffer, and faster.
# Must be greater than 0.
stiffness = 800

# ζ for a column's width — 1 settles without overshoot, less overshoots.
# Must be at least 0.
damping-ratio = 1

[animation.movement]

# Spring constant k for a window the strip rearranged — larger is stiffer, and faster.
# Must be greater than 0.
stiffness = 800

# ζ for a window the strip rearranged — 1 settles without overshoot, less overshoots.
# Must be at least 0.
damping-ratio = 1

[guide]

# What the guide draws for each window, or off.
# One of "off", "placeholder", "preview".
style = "off"

# Which corner or edge of the working area the guide sits at.
# One of "top-left", "top-center", "top-right", "center-left", "center", "center-right",
# "bottom-left", "bottom-center", "bottom-right".
position = "top-right"

# How wide the guide is at its longest, as a fraction of the working width.
# Must be greater than 0.
width = 0.2

# The most screens of strip it shows at once; a shorter strip shrinks it.
# Must be greater than 0.
span = 3

# Points held clear between the guide and the working area's edge.
# In points, must be at least 0.
gap = 24

# Seconds the guide stays up after the last thing that moved.
# In seconds, must be at least 0.
duration = 0.7

# The one open table: its names are chords you invent, and its values are commands spelled exactly
# as the CLI spells them. Nothing is bound by default — registering a hotkey takes that chord from
# every other app on the machine, which is also why `exec` is in the vocabulary: emira has to be
# able to give one back. Punctuation in a chord is named rather than typed.
[keys]

alt-h = "focus left"
alt-shift-h = "move-window left"
cmd-alt-period = "center-column"
alt-space = "exec ghostty"

# A list, so the header repeats, and the only place order in the file means anything: every matching
# rule applies, top to bottom, later ones overriding earlier ones field by field. A rule has to both
# match something and do something. Regular expressions are compiled when the file is read, so a
# broken one is a line number rather than a rule that quietly never fires.
[[window-rules]]

# …or app-id-regex / title / title-regex, all of which must match.
app-id = "com.tinyspeck.slackmacgap"
# Where a matching window starts…
workspace = "3"
# …and how wide it starts, on width-presets' scale.
width = 0.5

# Write a regex in a 'literal string': a "…" string would need every backslash doubled, and \d is
# not an escape this grammar admits at all.
[[window-rules]]

title-regex = 'Inspector'
# Off the strip entirely, overriding whatever emira made of the window's role.
float = true
